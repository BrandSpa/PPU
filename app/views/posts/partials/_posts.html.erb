<script id="post-template" type="text/x-handlebars-template">
<div class="img-container">
 {{#if gallery}}
  <img src="{{ gallery.img_name.url }}" alt="{{ title }}" class="hidden-xs">
  {{else}}
   <img src="{{ img_name.url }}" alt="{{ title }}" class="hidden-xs">
 {{/if}}
</div>

<div class="content">
	<h3><a href="posts/{{ slug }}">{{ title }}</a></h3>
	  <p> {{shortenText2 excerpt }}</p>
	  <div class="footer">
	  	<span class="date">{{dateFormat date format="D - M - YYYY"}}</span>
	  	<a href="#" class="share" data-toggle="modal" data-target="#share-modal"><%= t("share") %> <i class="fa fa-share-alt"></i></a>
	  </div>
</div>
</script>

<script id="post-main-featured-template" type="text/x-handlebars-template">
<div class="img-container">
 {{#if gallery}}
  <img src="{{ gallery.img_name.url }}" alt="{{ title }}" class="hidden-xs img-responsive">
  {{else}}
   <img src="{{ img_name.url }}" alt="{{ title }}" class="hidden-xs img-responsive">
 {{/if}}
</div>

  <div class="content">
	  <h3><a href="posts/{{ slug }}">{{ title }}</a></h3>
	  <p> {{shortenText excerpt }}</p>
	  <div class="footer">
	  <span class="date">{{dateFormat date format="D - M - YYYY"}}</span>
	  <a href="#" class="share" data-toggle="modal" data-target="#share-modal"><%= t("share") %> <i class="fa fa-share-alt"></i></a>
	  </div>
  </div>
</script>


	


