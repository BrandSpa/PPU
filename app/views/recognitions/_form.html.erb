<script type="text/x-handlebars-template" id="lawyer-form-recognition-template">
<form> 
<div class="row">
  <div class="form-group col-lg-4">
    <input type="text" name="fields[title]" class="form-control" placeholder="Título" value="{{ title }}">
  </div>

   <div class="form-group col-lg-2">
    <select name="fields[country]" class="form-control">
     {{#if country}}
        <option value="{{ country }}">{{ country }}</option>
      {{else}}
        <option value="">Seleccionar País</option>
      {{/if}}
      <%= render "partials/countries" %>
    </select>
  </div>

  <div class="form-group col-lg-2">
    <input type="text" name="fields[year]" class="form-control datepicker-year" placeholder="Año" value="{{ year }}">
  </div>
  </div>
  </form>
</script>