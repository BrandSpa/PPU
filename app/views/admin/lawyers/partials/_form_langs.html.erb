<div class="panel panel-default">
  <div class="panel-body">
    <h4><%= t('languages') %></h4>
    <hr>
    <div class="row">
      <div class="form-group col-lg-3">
        <select name="languages[][name]" class="form-control">
          <option value="">Seleccionar idioma</option>
          <%= render "partials/langs" %>
        </select>
      </div>

      <div class="form-group col-lg-3">
        <select name="languages[][name]" class="form-control">
          <option value="">Seleccionar idioma</option>
          <%= render "partials/langs" %>
        </select>
      </div>
</div>
      <div class="col-lg-12">
        <hr>
        <a href="#" class="btn btn-info pull-right lawyer-add-langs">+</a>
      </div>
    
  </div>
</div>

 <script type="text/x-handlebars-template" id="lawyer-form-langs-template"> 
 <div class="row">
  <div class="form-group col-lg-3">
    <select name="languages[][name]" class="form-control">
    <option value="">Seleccionar idioma</option>
      <%= render "partials/langs" %>
    </select>
  </div>

  <div class="form-group col-lg-3">
    <select name="languages[][name]" class="form-control">
    <option value="">Seleccionar idioma</option>
      <%= render "partials/langs" %>
    </select>
  </div>
  </div>
</script>